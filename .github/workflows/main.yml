name: ci
 on:
   push:
     branches:
       - main
   pull_request:
       - main
jobs:
   build:
      runs-on: ubuntu-latest
      - uses: actions/checkout@v3
      - runs: echo "running build..."
